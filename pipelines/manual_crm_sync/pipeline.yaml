name: Manual CRM Sync
description: "Salesforce → Snowflake"
runtime_engine: snowflake
schedule: every_6_hours
status: active
nodes:
  - type: extract
    label: "Read Salesforce Accounts"
    count: 1
  - type: transform
    label: "Standardize fields"
    count: 1
  - type: filter
    label: "Active accounts only"
    count: 1
  - type: load
    label: "Write to snowflake.crm.accounts"
    count: 1
